# 🧾 QR Code Generator (Python)

A simple Python project that generates a QR code from any user-provided URL or text input using the **qrcode** library.

---

## 🚀 Features
- Generates QR codes instantly.
- Accepts any text or URL input.
- Saves the QR code image automatically in the `static` folder.
- Lightweight and beginner-friendly.

---

## 🛠️ Requirements
Make sure you have **Python 3.8+** installed and the following package:

```bash
pip install qrcode[pil]